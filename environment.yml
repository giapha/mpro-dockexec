# NewScience Paper 1 — docking executability audit + re-execution env
# Recreate:  mamba env create -f environment.yml   (or: micromamba create -f environment.yml)
# Verified versions captured 2026-06-28 from the working env (/tmp/mm/envs/dock).
name: dock
channels:
  - conda-forge
dependencies:
  - python=3.11
  - rdkit            # 2026.x (conda-forge)
  - vina=1.2.7       # AutoDock Vina python bindings + CLI
  - meeko            # mk_prepare_ligand.py / mk_prepare_receptor.py
  - numpy=1.26
  - pandas
  - scipy=1.17
  - matplotlib=3.11
  - openpyxl=3.1
  - requests
# Full pinned pip list: see dock_env_pip_freeze.txt
